def say_hello(name):
    print("Hello, " + name)
    """
    Function to greet a person by name.
    
    Parameters:
    name (str): The name of the person to greet.
    
    Returns:
    str: A greeting message.
    """
    return f"Hello, {name}!"

say_hello("World")
